#include<bits/stdc++.h>
using namespace std;
int rnd1(double num) {
    return (num > 0.0) ? (int)(num + 0.5) : (int)(num - 0.5);
}
int rnd2(double num) {
    return (num > 0.0) ? (int)floor(num + 0.5) : (int)ceil(num - 0.5);
}
